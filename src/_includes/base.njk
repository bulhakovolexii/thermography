<!DOCTYPE html>
<html lang="{{ page.lang }}">
    <head>
        {% include "head.njk" %}
    </head>
    <body>
        {{ content | safe }}
        {% if diagrams %}{% mermaid_js %}{% endif %}
    </body>
</html>
